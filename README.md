# Custom Form Builder

A custom form builder built on React.js.

## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `bun install` or `npm install`
2. Run the app:
   `bun dev` or `npm run dev`
